function result(u,h,l,y)
{
    y= Math.log(l) + h + Math.pow(u,2);
    return y
}
let y1=result(9,8,15);
console.log(y1);
class App
{
    runApplication()
    {
        console.log("isg");
    }
}
let app = new App();
app.runApplication();